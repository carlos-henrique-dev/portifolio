import type {Metadata} from "next";import {ProjectCase} from "@/components/project-case";
export const metadata:Metadata={title:"Organizador Financeiro — Case | Carlos Henrique",description:"Case de produto remote-first com cache IndexedDB para gestão e planejamento financeiro familiar."};
export default function Page(){return <ProjectCase locale="pt"/>}
